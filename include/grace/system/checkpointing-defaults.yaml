checkpoints: {
  checkpoint_dir: "./checkpoints",
  max_n_checkpoints: 3,
  interval_kind: "walltime",
  iteration_interval: 1000,
  time_interval: 204,
  checkpoint_at_termination: true,
  checkpoint_at_startup: false,
  walltime_interval: 12 # hours 
}